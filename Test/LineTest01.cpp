#include <iostream>

using namespace std;

int main(void) {
    int msgCnt;
    int consumerCnt;
    cin >> msgCnt >> consumerCnt;
    
    int processTime[msgCnt];
    for(int i=0; i<msgCnt; i++)
        cin >> processTime[i];
    
    // cout << a + b << endl;
    return 0;
}