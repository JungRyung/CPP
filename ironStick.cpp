#include <string>
#include <vector>
#include <stack>
#include <queue>

using namespace std;

int solution(string arrangement) {
    int answer = 0;
    queue<char> q;
    for(int i=0; i<legth(arrangement); ++i)
    {
        
    }
    return answer;
}

int main()
{
	string arrangement = "()(((()())(())()))(())";
	solution(arrangement);
	return 0;
}