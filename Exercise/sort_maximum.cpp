#include <string>
#include <vector>
#include <algorithm>

using namespace std;

string solution(vector<int> numbers) {
    string answer = "";
    vector<int> temp;
    for(int i=0; )
    return answer;
}

int main(){
	vector<int> numbers;
	numbers.push_back(6);
	numbers.push_back(10);
	numbers.push_back(2);
}